<!--pages/history/history.wxml-->
<view class="title" >试卷记录</view>
<view wx:for="{{courseId}}">
  <view class="courseItem">
    <view class="courseId">{{item.id}}</view>
    <view class="courseName">{{item.name}}</view>
  </view>
</view>